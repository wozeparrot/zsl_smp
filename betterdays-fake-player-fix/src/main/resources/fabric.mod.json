{
  "schemaVersion": 1,
  "id": "betterdays-fake-player-fix",
  "version": "${version}",
  "name": "Better Days Fake Player Fix",
  "description": "Excludes Carpet fake players from Better Days sleep ratio calculations so time acceleration works correctly on servers with fake players.",
  "authors": ["woze"],
  "license": "MIT",
  "environment": "server",
  "depends": {
    "fabricloader": ">=0.19.3",
    "minecraft": "26.2",
    "betterdays": "*"
  },
  "mixins": [
    "betterdaysfakeplayerfix.mixins.json"
  ]
}
